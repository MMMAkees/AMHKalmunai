# Laragon MySQL defaults — copy to .env and adjust if needed
# cp .env.example .env

# Server
PORT=5000
NODE_ENV=development

# Laragon MySQL (default)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=amh_kalmunai

# JWT (Phase 3 — used by auth endpoints)
JWT_SECRET=amh_kalmunai_change_this_in_production
JWT_EXPIRES_IN=7d

# Frontend URL (CORS)
CLIENT_URL=http://localhost:5173
